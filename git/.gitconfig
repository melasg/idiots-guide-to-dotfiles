[include]
        # path = ~/.gitconfig.local
        path = ~/git/.gitconfig
[hub]
        protocol = https
[github]
		user   = melasq

[user]
		name = Melody
		email = melodymelika@protonmail.ch

[mergetool]
		keepBackup = false

[alias]
		# parent = rev-parse --abbrev-ref --symbolic-full-name @{u}
		# last = log -1 HEAD
		# unstage = reset HEAD --
		# lazy = !lazygit
		# contributors = shortlog --summary --numbered

[color]
		diff = auto
		status = auto
		branch = auto
		ui = true

[commit]
		gpgsign = false

[core]
		editor = vim
		excludesfile = ~/.gitignore
	
[credential]
		helper = osxkeychain

[apply]
        	whitespace = nowarn

[mergetool]
        	keepBackup = false
			
[difftool]
        	prompt = false

[help]
        	autocorrect = 1
[push]
        # See `git help config` (search for push.default)
        # for more information on different options of the below setting.
        #
        # Setting to git 2.0 default to suppress warning message
        default = simple